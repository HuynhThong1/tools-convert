# Optional: YouTube OAuth Token (Recommended to avoid bot detection errors)
# This helps bypass YouTube's "Sign in to confirm you're not a bot" error
#
# How to get an OAuth token:
# 1. Go to https://developers.google.com/oauthplayground/
# 2. In "Step 1", find and select "YouTube Data API v3" -> All available scopes
# 3. Click "Authorize APIs" and sign in with your Google account
# 4. In "Step 2", click "Exchange authorization code for tokens"
# 5. Copy the "Access token" value
# 6. Add it to your Vercel environment variables or .env.local file
#
# Note: OAuth tokens expire after 1 hour by default. For production use,
# you may need to implement token refresh or use a service account.
#
# YOUTUBE_OAUTH_TOKEN=your_oauth_token_here

# Optional: Add any custom configuration here if needed in the future
# NEXT_PUBLIC_APP_URL=http://localhost:3000
